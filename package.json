{
  "name": "lyricsync",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Offline-first desktop audio-text synchronization foundation",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "desktop": "electron .",
    "audit-library": "node scripts/audit-library.mjs",
    "create-review-queue": "node scripts/create-review-queue.mjs",
    "validate-review-queue": "node scripts/validate-review-queue.mjs",
    "test": "node --test"
  },
  "main": "desktop/main.cjs",
  "devDependencies": {
    "electron": "^37.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
